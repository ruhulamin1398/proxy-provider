services:
  - type: web
    name: proxy-provider
    env: go
    buildCommand: go build -tags netgo -ldflags '-s -w' -o app ./cmd/server
    startCommand: ./app
    envVars:
      - key: PORT
        value: 8080